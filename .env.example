# Copy to .env.local and fill in values

GEMINI_API_KEY=  # get from https://aistudio.google.com/app/apikey

# Photo ingest: set true to skip Gemini and use demo-photo-identified.json (conference Wi‑Fi).
# DEMO_PHOTO_MOCK=false

# Optional: set to 1 while running npm run dev:https to register the PWA service worker on Android/Chrome during local testing.
# NEXT_PUBLIC_PWA_DEV=1

# Add any other API keys or secrets here
